# rsrch.space
Ishan's favorite links.

## Notes
We can't update the Python dependencies, we don't know how. More seriously, Notion deprecated the database query endpoint (https://developers.notion.com/reference/post-database-query) and I don't want to migrate, so we're going to stick with this.

Run this with `python main.py`.